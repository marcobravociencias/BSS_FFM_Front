<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.totalplay.ffm.plantaexterna.repository.OperarioPERepository">
 	
 	<select id="obtenerOperarios" resultType="com.totalplay.ffm.plantaexterna.model.OperarioPEVO"
		parameterType="java.util.Map">						    
			    SELECT PIR_COMENTARIO  AS NEMPLEADO FROM PLE_INCIDENTS_ROOT WHERE PIR_ID <![CDATA[ < ]]> 100					
	</select>

</mapper>